/// <reference types="vite/client" />

/**
 * Loads Vite's ambient asset declarations so TypeScript recognizes the
 * library and demo's side-effect CSS imports, including when editors enable
 * `noUncheckedSideEffectImports`. This lives under `vite/` because `src/` is
 * intentionally reserved for reusable library modules under `src/lib`.
 */
